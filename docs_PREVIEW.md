# UI Preview Targets

After `npm run dev`, capture these routes:
- `/dashboard` (Admin Dashboard)
- `/crm` (CRM)
- `/follow-ups` (Follow-Ups)
- `/` (Public Homepage)

Use your local browser screenshot tool and save into `docs/screenshots/`.
